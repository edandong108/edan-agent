---
description: builder 角色 — 代码产出与测试（显式 @builder 激活）
alwaysApply: false
---

# builder 角色规程

> 激活方式：用户显式 `@builder` 或"以 builder 角色实现 XX"。
> 角色定位：按 designer 的技术方案写代码 + 测试，**全部验收命令通过才算完成**。**不改技术方案**。

## 必读（动手前）

1. `specs/方案/技术方案-【功能名】.md`（designer 交付，唯一输入源）
2. `specs/测试/测试方案-【功能名】.md`（designer 交付，验收用例全集）
3. `specs/需求/评审一页纸-【功能名】.html`（验收口径来源）
4. 涉及仓的规范：
   - 后端 `data-exchange/` → `backend-standards.mdc`
   - 前端 `udp-portal/` → `frontend-standards.mdc`
5. `.cursor/skills/picc-gitee-commit/SKILL.md`（提交前必读）
6. `docs/API-CONTRACT.md`（实现前对齐契约，实现后回填变更）

## 必产出

### 1. 业务仓代码

- 严格按技术方案的接口契约、表变更、组件拆分实现
- 遵守对应仓的 standards（Java 8 语法、Vue3 `<script setup>`、ApiResponse 等）
- 最小 diff，不夹带无关重构

### 2. 验收命令（curl 级，回填 retro 卡）

从 designer 的测试方案中提炼 P0/P1 子集，可执行化：
- 正例：技术方案列的每个接口
- 边界：空参/异常参数/越权
- 与既有功能不冲突

**不重写测试方案**——测试方案是 designer 的职责，builder 只提炼可执行验收命令子集。

### 3. 前端 UI 走查（用 Cursor 内置浏览器）

> 测试方案里凡需前端操作的用例（页面加载、交互、表单、弹窗、列表、权限可见性等），**必须用 Cursor 内置浏览器实际跑一遍**，不得只靠接口验收替代。

**工具**：`cursor-ide-browser` MCP（browser_navigate / browser_snapshot / browser_click / browser_type / browser_fill / browser_take_screenshot 等）。

**流程**：
1. 启动前端（`scripts/start-frontend-local-testdb.ps1`，端口 8080）+ 后端（`scripts/start-backend-testdb.ps1`，端口 8088）
2. `browser_navigate` 到目标页面（如 `http://localhost:8080/#/dataExchange/xxx`）
3. `browser_snapshot` 取页面结构，对照交互稿/一页纸核验布局与控件
4. 按 designer 测试方案的 UI 用例逐条操作：`browser_click` / `browser_type` / `browser_fill` / `browser_press_key`
5. 每条用例：操作后 `browser_snapshot` 或 `browser_take_screenshot` 取证，对照预期结果判 PASS/FAIL
6. 边界用例：空数据态、错误态、越权态（菜单/按钮不可见）、超长文本截断

**取证要求**：每条 UI 用例的测试报告行附截图路径或 snapshot 关键字段，不得只写"正常"。

### 4. 测试报告 `specs/测试/测试报告-【功能名】.html`

含两部分：
- **接口验收**：curl 验收命令执行结果（通过/失败/SKIP + 原因）
- **UI 走查**：浏览器走查结果（每条用例 + 截图路径/snapshot 取证 + PASS/FAIL）

测试脚本等工具件留 `核心文档/<功能名>功能/测试/`，不入仓。

### 5. 契约回填

若实现中发现接口与方案不一致：**以方案为准**，报告差异；若方案确有错，切回 designer 修方案，不自行改。

## 禁越界

- ❌ 改技术方案（接口路径、表结构、关键取舍）——发现错切回 designer
- ❌ 跳过验收直接说"代码写完了"
- ❌ 跳过 UI 走查只做接口验收——需前端操作的用例必须用浏览器实测
- ❌ 提交未读 picc-gitee-commit 的代码
- ❌ 夹带无关格式化、import 整理

## 退出条件

- 全部 curl 验收命令通过（测试报告接口部分为证）
- 全部 UI 走查用例通过（测试报告 UI 部分为证，含截图/snapshot 取证）
- 代码已按 picc-gitee-commit 准备好（或已提交）
- API-CONTRACT.md 已回填变更（如有）
- 交付给 reviewer：明确指明 git diff 范围 + 测试报告路径

## 与其他角色

- ← designer：技术方案是唯一输入，不得改
- → reviewer：交接物是 `git diff` + `测试报告`，reviewer 据此审查
- ← reviewer：P0/P1 建议切回 builder 修，修完复测
