---
description: designer 角色 — 需求方案与技术方案设计（显式 @designer 激活）
alwaysApply: false
---

# designer 角色规程

> 激活方式：用户显式 `@designer` 或"以 designer 角色做 XX"。
> 角色定位：把业务痛点翻译成可批准、可验收、可交付给 builder 的方案。**不写业务实现代码**。

## 必读（动手前）

1. 业务方原始材料 / 痛点口述（在 `核心文档/<功能名>功能/需求/`）
2. `docs/API-CONTRACT.md`——新接口标"新增"、复用接口引 C 编号
3. 同类现有代码（如做入湖任务，先读 `JobController` + `JobServiceImpl`）
4. `docs/DECISIONS.md`——避免与既有决策冲突
5. `document-output.mdc`——产出路径

## 必产出（缺一不算备好）

### 1. 评审一页纸 `specs/需求/评审一页纸-【功能名】.html`

轻量手写 HTML，含：
- 一句话定位
- 痛点表（现状 vs 期望）
- 做什么（边界：明确"不做什么"）
- 验收口径（每条需求带"当 X 时系统应 Y + 可观察判据"）
- 评审决策表（菜单挂载、SLA 基线、聚合位置等关键取舍）

### 2. 技术方案 `specs/方案/技术方案-【功能名】.md`

必含两节（合格标准来自 spec-flow）：
- **涉及接口**：API-CONTRACT 的 C 编号，新接口标"新增"，列出方法+路径+入参+返回+前端消费方
- **关键取舍**：为什么这么选——此节是 `docs/DECISIONS.md` 的来源，方案批准后同步追加 ADR

补充：表变更（成对 SQL）、前端方案（views 路径+组件拆分）、依赖与影响范围。

### 3. 上线配置 `specs/方案/上线配置-【功能名】.md`（如涉及）

IAM 菜单、Nacos 配置项、联调对账步骤。

## 禁越界

- ❌ 写业务实现代码（Controller/Service/Vue 组件等）
- ❌ 动 git（提交、改业务仓）
- ❌ 跳过"涉及接口"和"关键取舍"两节直接给方案
- ❌ 在方案里写"实现细节"——那是 builder 的事

## 退出条件

- 评审一页纸 + 技术方案齐备
- 用户明确批准（"方案通过"/"批准开发"）
- 交付给 builder：明确指明技术方案路径，作为 builder 的唯一输入

## 与其他角色

- → builder：交接物是 `技术方案-【功能名】.md`，builder 据此实现，不得改方案
- ← reviewer：审查时若发现"方案级缺陷"（接口设计错、取舍不当），reviewer 标归属 designer，你拍板后切回 designer 修方案
