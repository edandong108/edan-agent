---
description: 产出材料统一存放至核心文档目录
alwaysApply: true
---

# 产出材料存放规范

> 本规则只管**过程稿与原件归档**：理解确认、产品交互稿、测试脚本、飞书导出件、PDF 原件等，统一存放核心文档区。
> **定稿材料**（评审一页纸/技术方案/测试方案/迭代卡）按 `spec-flow.mdc` 的材料组织规则入本仓 `specs/`，不受本规则管辖。

本项目中，由 AI 助手产出的过程稿与原件，统一存放至：

```
D:\自助编程\实时配置自动化\核心文档\
```

## 适用范围

以下类型的**过程稿与原件**应写入该目录（定稿入 `specs/`，见 spec-flow）：

- 理解确认、产品交互稿（HTML）、测试脚本等过程件
- 飞书导出件、PDF 原件、二进制原件
- 汇报材料、总结文档、会议纪要草稿
- 排障记录、运维台账、操作手册
- 其他非代码类的文档产出

## 执行要求

1. 新建文档时，默认路径为 `核心文档/<功能名>功能/`（如 `核心文档/SLA功能/方案/`），不要散落在项目根目录或其他位置
2. 功能内固定三层：`需求/`（业务方原始材料）、`方案/`（团队技术产出）、`测试/`；排障等可另建子目录
3. **禁止**在 `核心文档/` 外层建 `方案/`、`需求/` 等功能材料目录
4. **可复用**材料（本地联调模板、部署手册等）放在 `核心文档/` 外层
5. 已交付功能整体移入 `核心文档/归档/`
6. 代码文件仍按原有仓库结构存放（`data-exchange/`、`udp-portal/`），不受此规则影响
7. 用户未另行指定路径时，优先使用 `核心文档/` 目录；索引见 `核心文档/README.md`
