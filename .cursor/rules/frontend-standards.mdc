---
description: 前端开发规范（udp-portal）
globs: udp-portal/**/*
alwaysApply: false
---

# 前端开发规范

权威来源：`相关规范/前端规范.doc`。以下为 udp-portal 开发时必须遵守的要点。

## 技术栈

- Vue3 + TypeScript 5 + `<script setup lang="ts">`（唯一标准写法，禁止 Options API）
- Vite 构建；UI 使用 **Ant Design Vue**（本项目现状）
- 状态：Pinia（全局）+ ref/reactive（局部）；复杂逻辑抽 hooks
- 请求：统一 `@/utils/request`，**禁止裸写 axios/fetch**
- 样式：SCSS/Less，BEM 命名，禁止大量行内 style
- ESLint + Prettier，2 空格缩进，UTF-8

## 目录结构（新建文件遵守）

```
src/
├── api/           # 按业务模块拆分，带 TS 类型
├── components/
│   ├── common/    # 全局通用组件
│   └── business/  # 业务通用组件
├── views/         # 页面模块，kebab-case
│   └── xxx/
│       ├── index.vue
│       ├── types.ts
│       ├── hooks/
│       ├── components/
│       └── index.scss
├── hooks/         # 全局 hooks
├── stores/        # Pinia（本项目为 stores/）
├── types/         # 全局类型、枚举
├── utils/
└── router/
```

## 命名

| 类型 | 规则 | 示例 |
|------|------|------|
| 页面/文件夹 | kebab-case | `task-management` |
| 组件文件 | PascalCase | `MetricCard.vue` |
| 接口/类型 | I 前缀 | `IMetricItem` |
| 变量/函数 | camelCase | `getMetricPage` |
| 常量 | UPPER_SNAKE_CASE | `MAX_EXPORT_SIZE` |
| 布尔 | is/has/can 前缀 | `isShowModal` |

## TypeScript（最高优先级）

- `strict: true`，严禁 `any`
- Props、Emits、接口入参/返回值全部显式类型
- 禁止滥用 `!`；用 `?.`、`??` 处理空值
- 参数超过 3 个封装对象类型
- 枚举与后端一一对应，禁止硬编码数字状态

## Vue3 组件

- 结构顺序：`<script setup>` → `<template>` → `<style scoped>`
- Props：`defineProps` + `withDefaults`，完整类型
- Emits：`defineEmits` 严格类型；双向绑定用 `update:xxx`
- 基础类型用 `ref`，对象/数组用 `reactive`
- 分页：`reactive({ pageNum, pageSize, total })`
- 加载态：`loading = ref(false)`，提交用 `submitLoading`
- 派生数据用 `computed`，复杂 watch 抽入 hooks
- **单文件超 300 行强制拆分**（hooks、子组件、types）

## 组件化抽取（主动识别，避免过早抽象）

> 与"300 行强制拆分"互补：300 行是被动红线，本节是主动抽取准则。
> 业界依据：Rule of Three（Martin Fowler）+ Atomic Design（Brad Frost）+ YAGNI。

### 识别信号（满足任一即考虑抽取）

| 信号 | 判据 | 动作 |
|------|------|------|
| 同样布局/交互出现 ≥2 次 | 同样的卡片样式、弹窗结构、表格+分页+筛选壳 | 标记 TODO，观察是否稳定 |
| 同样数据形态出现 ≥2 次 | 同一 DTO 在多页面渲染成同样的列表/详情 | 抽组件 + 共享 types |
| 单文件超 300 行 | （已有规则） | 强制拆 hooks/子组件/types |
| 一段逻辑被复制粘贴 | 即使只出现 2 次，复制即信号 | 抽 hooks 或组件 |

### 三次法则（避免过早抽象）

- 出现 **2 次**：标记 TODO，先观察是否真稳定（业务易变的不急抽）
- 出现 **3 次**：抽取，放到合适层级
- 出现 **5 次以上**：检查是否该上提到更通用层（business → common）

### 抽取层级（按复用范围，不按"看起来通用"）

| 复用范围 | 放哪 | 示例 |
|---------|------|------|
| 单页面内 | `views/xxx/components/` | StatCard、SliceChart（SLA 运维页专用） |
| 多页面共享、带业务语义 | `components/business/` | TaskStatusBadge、ApprovalTimeline |
| 多页面共享、纯 UI 无业务 | `components/`（现有全局层） | BaseTable、BaseCharts、InputSelect |
| 跨项目通用 | 不在本项目抽，提 issue 到公共组件库 | — |

**判断顺序**：先问"复用范围"，再选层；不确定先放 `views/xxx/components/`，复用时再上提（下移易、上提难）。

### 组件设计准则

- **单一职责**：一个组件只做一件事；既渲染数据又管请求的，拆成"展示组件 + hooks"
- **props 单向、emit 上报**：子组件不直接改父状态，靠 emit 通知
- **slot 优先于 props 渲染函数**：插槽比 `renderProp` 更 Vue 风格
- **Ant Design Vue 优先**：不重复造按钮/输入框/表格，先看 AntD 是否有
- **types 共享**：跨组件用的类型放 `views/xxx/types.ts` 或 `types/`，不散落各组件

### 不抽的边界（防止过度抽象）

- 只出现 1 次的"看起来通用"代码——不抽，YAGNI
- 业务逻辑易变的组件——先内聚在页面，等稳定再抽
- 为了"统一"强行抽的抽象层——抽象比重复更贵，重复两次的修正成本远低于错误抽象的修正成本

## 接口请求

- api 文件按模块导出，绑定 TS 类型
- 全局 request 拦截 401/500/业务错误，组件内不重复弹窗
- 导入顺序：内置 → 第三方 → 全局 hooks/store/utils → 本地
- 使用 `@/` 别名，禁止多层 `../../`
- 禁止 `console.log` 提交（调试用 warn，上线前删除）

## Pinia & Hooks

- store 按业务分模块，setup 写法，异步放 action
- 页面临时状态不进 Pinia
- 通用逻辑 → `@/hooks`；页面独有 → `views/xxx/hooks/useXxx.ts`
- hook 以 `use` 开头，不操作 DOM

## 样式

- BEM：`block__element--modifier`
- 颜色/间距用全局 SCSS 变量，禁止写死色值
- 页面样式独立 `.scss` 文件

## 接入现有项目

改 `dataExchange/taskManagement` 等模块时，**先读现有 `edit.vue`、`dataChange.ts` 写法再动手**，保持与现网一致。
