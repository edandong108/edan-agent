---
description: 本机联调启动自愈 — 禁止裸 mvn，统一走 scripts 与 ensure-local-backend
alwaysApply: true
---

# 本地联调启动（自愈）

与「提交前纪律」配合：`picc-gitee-commit` skill 管 **push 前不夹带 Local***；本规则管 **启动前自动恢复可编译、可跑 local-testdb 的本机环境**。

## Agent 必须遵守

当用户要求**启动 / 重启 / 联调**前后端，或 builder 验收前需要本机服务时：

1. **禁止**在 `data-exchange/` 下直接执行 `mvn spring-boot:run`（会缺 `local-build`、易与 `Local*` Bean 冲突）。
2. **必须**使用仓库根目录脚本（脚本内已调用 `scripts/ensure-local-backend.ps1`）：

| 场景 | 命令 |
|------|------|
| 默认（连公司测试库 + mock 前端） | `scripts\start-backend-testdb.ps1` |
| 前端配套 | `scripts\start-frontend-local-testdb.ps1` |
| 早期本机双库 `local` profile | `scripts\start-backend.ps1` + `scripts\start-frontend.ps1` |

3. 在**后台**启动时仍用上述 `.ps1` 路径，勿手写省略 `-Plocal-build` 的 Maven 命令。
4. 启动后自检：`GET http://localhost:8088/actuator/health` → UP；前端 `http://localhost:8080`。
5. 任务/队列等依赖 MySQL 副库时，提醒用户连 **VPN**（`10.57.4.6:6033`）。

## ensure-local-backend 做什么（无需手跑）

`scripts/ensure-local-backend.ps1` 幂等执行：

- 缺 `pom.xml` 的 **`local-build`** profile 时，从锚点提交恢复 Hadoop 编译捷径
- 缺 `Local*` 类时从锚点恢复
- 为 `DatasourceConfig` / `SecondaryDatasourceConfig` / `WebConfig` 补 **`@Profile("!local & !local-testdb")`**（避免与 `LocalWebConfig` 等抢 Bean）
- 缺 `application-local-testdb.properties` 时从 example 复制

## 用户可选：提交后自愈

一次性安装 git hook（仅本机 `.git/hooks`，不进仓）：

```powershell
核心文档\本地联调\install-dev-hooks.ps1
```

`data-exchange` 每次 **commit 后**自动再跑 `ensure-local-backend.ps1`。提交前仍按 skill 做 `git restore`，提交后不必记得手动恢复。

## 详细说明

`核心文档/本地联调/README-本地开发说明（勿提交仓库）.md`
