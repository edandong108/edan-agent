# NNN-<功能名> · 需求（requirements）

> EARS 格式：每条「当 X 时，系统应 Y」，带验收标准。你口述 → agent 起草 → 你改两轮定稿。
> 定稿后 agent 只许实现本文件有的，不许自由发挥。

## 背景与目标

<一两句话：谁在什么场景下遇到什么问题>

## 需求条目

| 编号 | 需求（EARS） | 验收标准 |
|------|-------------|---------|
| R1 | 当 <触发条件> 时，系统应 <行为> | <可观察的判据，如：调用 X 返回 Y> |
| R2 | | |

## 范围外（明确不做）

- <本期不做的，防止范围蔓延>

## 涉及接口（契约引用）

- 新增/变更：docs/API-CONTRACT.md 的 C 编号（新接口标"新增"）

## 审批

- [ ] 需求定稿（<姓名>，日期）
