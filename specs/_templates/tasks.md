# NNN-<功能名> · 任务（tasks）

> 执行契约：agent 按单执行，只做清单里的事。一页以内。
> 实现完成 = 全部验收命令通过，而非"代码写完了"。
> 交付后本文件价值即尽——验收结论由 retro 迭代卡沉淀。

| # | 任务 | 验收命令 | 依赖 |
|---|------|---------|------|
| 1 | <做什么> | `curl ...` 应返回 ... | — |
| 2 | | | #1 [P] |

> [P] = 可与其他 [P] 任务并行（多 agent 时各守边界，人守合并门）
